# Synonyms Quiz — οδηγός για μελλοντική επεξεργασία

## Τι είναι

Στατικό HTML κουίζ συνωνύμων στα Ελληνικά για μαθητές **ΣΤ΄ Δημοτικού** που προετοιμάζονται για εξετάσεις **Α΄ Γυμνασίου**. Σχεδιαστικός στόχος: απλό, minimal, ευχάριστο. Καμία εξάρτηση, κανένα build step.

## Δομή φακέλου

```
synonyms_quiz/
├── index.html        Σκελετός σελίδας
├── style.css         Στυλ (mobile-first, system fonts)
├── script.js         Λογική κουίζ (vanilla JS, καμία βιβλιοθήκη)
├── questions.json    Source of truth — όλες οι ερωτήσεις
└── CLAUDE.md         Αυτό το αρχείο
```

Αυτά τα 5 αρχεία είναι αυτάρκη — ολόκληρο το site. Δεν υπάρχει build pipeline, δεν υπάρχουν παραγόμενα αρχεία.

> Σημείωση: Στον αδελφό φάκελο `synonyms_quiz_archive/` βρίσκονται τα αρχικά `raw.md` και `parse.py` που χρησιμοποιήθηκαν μία φορά για το αρχικό seed των ερωτήσεων. **Δεν χρησιμοποιούνται από το site και δεν πρέπει να επανέλθουν στη ροή.** Οποιαδήποτε αλλαγή στις ερωτήσεις γίνεται απευθείας στο `questions.json`.

## Πώς διαβάζονται τα δεδομένα

Το `script.js` καλεί `fetch("questions.json")` κατά την εκκίνηση. Όλο το data flow περνάει από εκεί. Δεν υπάρχει inline embed, δεν υπάρχει fallback.

**Συνέπεια**: το site χρειάζεται να σερβίρεται από web server (GitHub Pages, οποιοδήποτε static host, ή τοπικός Python server). **Δεν δουλεύει με διπλό κλικ σε `file://`** — οι browsers μπλοκάρουν το `fetch` εκεί. Αν συμβεί, η σελίδα εμφανίζει σαφές μήνυμα με την εντολή για τοπικό server.

## Δομή ερωτήματος

```json
{
  "id": 1,
  "sentence": "Η κίνηση των πλανητών γύρω από τον ήλιο είναι ένας **αέναος** κύκλος.",
  "word": "αέναος",
  "options": ["προσωρινός", "αδιάκοπος", "περιστασιακός", "σύντομος"],
  "correct": "αδιάκοπος",
  "definition": "αυτός που δεν έχει τέλος, ο παντοτινός, ο συνεχής."
}
```

Σταθεροί κανόνες (το `script.js` τα προϋποθέτει):
- Πάντα **ακριβώς 4** options.
- Το `correct` πρέπει να υπάρχει αυτολεξεί στις `options` (case-sensitive).
- Η `sentence` πρέπει να περιέχει το token `**…**` ακριβώς μία φορά. Το περιεχόμενο μέσα στα `**` είναι αυτό που εμφανίζεται bold.
- Καμία `word` δεν πρέπει να επαναλαμβάνεται σε όλο το αρχείο (ο χρήστης το έχει ζητήσει ρητά). Δεν υπάρχει αυτόματος έλεγχος — χειροκίνητη προσοχή κατά την προσθήκη.
- Τα `id` δεν χρησιμοποιούνται σε external references — αρκεί να είναι μοναδικά. Όταν προσθέτεις, βάλε επόμενο διαθέσιμο ακέραιο.

## Πώς να προσθέσεις ή να διορθώσεις ερωτήματα

Επεξεργάζεσαι **απευθείας το `questions.json`**. Είναι ένα JSON array — προσθέτεις/αλλάζεις records, σώζεις, commit, push. Αν το site τρέχει ήδη σε browser, ένα refresh φέρνει τις αλλαγές (το script φορτώνει με `cache: "no-cache"`).

Quick checklist για νέο ερώτημα:
1. Νέο `id` (επόμενος αριθμός).
2. Πρόταση με ακριβώς ένα `**bold**` token.
3. Το `word` ίδιο με ό,τι είναι μέσα στα `**…**` (ίδια κλίση, π.χ. "πενιχρό" και όχι "πενιχρός" — όπως εμφανίζεται στην πρόταση).
4. 4 options. Το `correct` πρέπει να ταιριάζει αυτολεξεί με ένα από αυτά.
5. Σύντομος `definition` (μία φράση, μικρό γράμμα στην αρχή — έτσι είναι ο τόνος όλου του dataset).

## Τοπική προεπισκόπηση

```bash
cd /path/to/synonyms_quiz
python3 -m http.server 8765
# Άνοιξε http://127.0.0.1:8765/
```

## Deployment σε GitHub Pages

1. `git init` στον φάκελο, commit τα 5 αρχεία.
2. Push σε public repo.
3. Settings → Pages → Source = `main` branch, root folder.
4. Το URL είναι `https://<user>.github.io/<repo>/`.

Δεν χρειάζεται build / CI / package.json / οτιδήποτε άλλο.

## Συμπεριφορά κουίζ

- Σελίδες των `BATCH_SIZE = 10` ερωτήσεων (η τελευταία μπορεί να έχει λιγότερες ανάλογα με το πλήθος ερωτήσεων). Στο UI εμφανίζονται ως **«σελίδες»** — ο όρος «δεκάδα» δεν χρησιμοποιείται πλέον σε χρήστη-facing strings (αν κρατηθεί `BATCH_SIZE` διαφορετικό από 10, το όνομα παραμένει «σελίδα»).
- Το κουμπί **Έλεγχος απαντήσεων** μένει disabled μέχρι να απαντηθούν όλα τα ερωτήματα της σελίδας.
- Μόνο μετά τον έλεγχο: highlight σωστού (πράσινο) και επιλεγμένου-λάθος (κόκκινο). Πάνω από το footer εμφανίζεται κουτί «Λέξεις που σε δυσκόλεψαν» με `word → correct` + `definition`.
- Στο τέλος όλου του κουίζ: συνολικό σκορ + ποσοστό + συγκεντρωτική λίστα όλων των λάθος (ταξινομημένη κατά `id` για σταθερή ροή ανάγνωσης) + κουμπί επανάληψης.
- Οι 4 επιλογές κάθε ερώτησης γίνονται **shuffle σε κάθε εκκίνηση** (page load ή κουμπί επανάληψης). Η σειρά των ερωτημάτων μένει σταθερή (όπως στο JSON).
- Καμία κατάσταση δεν αποθηκεύεται (no localStorage / no sessionStorage). Refresh = ξεκινάει από την αρχή. Η ελεύθερη πλοήγηση παρακάτω είναι **εντός του ίδιου session** μόνο.

### Ελεύθερη πλοήγηση μεταξύ σελίδων

Ο χρήστης μπορεί να κινηθεί ελεύθερα ώστε αν μια στιγμή απομακρυνθεί από μια σελίδα να μην ξαναλύσει ό,τι έχει ήδη απαντήσει.

- **Footer πλοήγηση**: σε κάθε σελίδα εμφανίζονται «← Προηγούμενη» (αριστερά, αν `batchIndex > 0`) και «Επόμενη →» (δεξιά, αν δεν είναι η τελευταία). Η πρωτεύουσα ενέργεια στο κέντρο εξαρτάται από την κατάσταση της σελίδας:
  - Ανέλεγκτη: «Έλεγχος απαντήσεων» (disabled με metric `(X/N)` μέχρι να απαντηθούν όλες).
  - Ελεγμένη + όχι όλες οι σελίδες ελεγμένες: η πρωτεύουσα ενέργεια είναι το «Επόμενη σελίδα →» δεξιά (κανένα κεντρικό κουμπί).
  - Όλες οι σελίδες ελεγμένες: «Δες τα τελικά αποτελέσματα» στο κέντρο (διαθέσιμο από οποιαδήποτε ελεγμένη σελίδα).
- **Page strip (μόνιμο, sticky)**: ακριβώς κάτω από τον τίτλο της σελίδας υπάρχει οριζόντια λωρίδα με **όλα** τα αριθμημένα κουτάκια σελίδων. Είναι `position: sticky; top: 0` — μένει ορατή καθώς ο χρήστης κάνει scroll μέσα στη σελίδα. Σε στενές οθόνες κυλάει οριζόντια· το `is-current` κουτάκι κάνει `scrollIntoView({inline:"center"})` σε κάθε αλλαγή σελίδας. Χρωματισμός κατά κατάσταση: `empty` (άθικτη), `partial` (σε εξέλιξη), `complete` (όλες απαντημένες χωρίς έλεγχο), `reviewed` (ελεγμένη). Από κάτω σταθερό υπόμνημα. Δεν υπάρχει πλέον popup/dropdown — το strip χτίζεται μία φορά (`buildPageStrip`) και ενημερώνεται in-place (`updatePageStrip`).
- **Selections persistence**: όλες οι απαντήσεις φυλάσσονται σε `state.selections` (Map by `q.id`) και επιβιώνουν την πλοήγηση μεταξύ σελίδων. Το αν μια σελίδα είναι "answered/complete" προκύπτει on-the-fly από αυτό.
- **Reviewed lock**: μόλις πατηθεί «Έλεγχος» η σελίδα μπαίνει στο `state.reviewed` Set και κλειδώνει — read-only με highlights, δεν αλλάζουν απαντήσεις, δεν ξαναελέγχεται. Επομένως `state.totalAnswered` και `state.wrongAll` αυξάνουν μόνο μία φορά ανά σελίδα.
- **Progress bar**: γεμίζει με `state.reviewed.size / state.batches.length`, όχι με τη θέση του χρήστη. Έτσι αντικατοπτρίζει πραγματική πρόοδο ανεξάρτητα από τη σειρά πλοήγησης.

## Σχεδιαστικές αποφάσεις (άσε ως έχει εκτός αν υπάρχει λόγος να αλλάξει)

- Vanilla JS / CSS, καμία εξωτερική εξάρτηση — λειτουργεί ακόμα και offline μετά το πρώτο load.
- System fonts για ταχύτητα και αναγνωσιμότητα στα Ελληνικά.
- Χρώμα accent: μπλε (`--accent: #2f6feb`). Σωστές πράσινες, λάθος κόκκινες, δύσκολες λέξεις σε ανοιχτό κίτρινο card.
- Mobile-first με breakpoint στα 520px.
- Bold λέξη στη πρόταση χρωματίζεται με το accent για να ξεχωρίζει.
- Aριθμός ερώτησης δεξιά-αριστερά (Α, Β, Γ, Δ) στα bullets, αλλά το data είναι ανεξάρτητο γράμματος — η αντιστοίχιση γίνεται κατά το rendering.
